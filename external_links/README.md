# External Links

This folder holds the symlinks of the files that have been provided as links.
With this indirection we can freely move the files around in the repo without worry of invalidating the shared links.
However, we need to manually update the symlinks here when the files are moved.
